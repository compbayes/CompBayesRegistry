## Local julia repository for the CompBayes group.

To add the registry to Julia do the following in Julia's package manager (press `]` to enter pkg mode):
```
registry add https://github.com/compbayes/CompBayesRegistry.git
```
